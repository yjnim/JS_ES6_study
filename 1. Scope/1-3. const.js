function home() {
    const homename = "my house";
    homename = "your house";
    //case1 : const는 변경할 수 없는 값이라서 error가 난다.

    console.log(homename);

    /* 코드를 작성할 땐 const를 기본으로 사용하되,
    변경이 될 수 있는 변수는 let을 사용한다
    var는 사용하지 않는다. */
}